# Builds the user-manual site (docs/Manual via MkDocs Material, see mkdocs.yml)
# and deploys it to GitHub Pages at https://matrikelhelfer.niggl-schlagbauer.de/.

name: Deploy manual to GitHub Pages

# On-demand only. Pushing docs/Manual/** keeps the manual SOURCE current in
# the repo (available on every machine) but deliberately does NOT publish, so
# the live site never describes unreleased features. Deploy to the public Pages
# site at release time: "Run workflow" in the Actions tab, or
#   gh workflow run manual.yml
on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install mkdocs-material
      - run: mkdocs build --strict
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
